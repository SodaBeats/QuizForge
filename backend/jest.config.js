// jest.config.js
// Default entrypoint for Jest. It points to the unit suite so local
// test runs remain lightweight and do not require a database.
import unitConfig from './jest.config.unit.js';

export default unitConfig;
